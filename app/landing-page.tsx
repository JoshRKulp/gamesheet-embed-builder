import Link from "next/link";

type LandingPageProps = { eyebrow: string; title: React.ReactNode; description: string; features: string[]; steps: string[] };

export default function LandingPage({ eyebrow, title, description, features, steps }: LandingPageProps) {
  return <main className="landing-shell"><header className="topbar landing-topbar"><Link className="brand" href="/"><span className="brand-mark">⌘</span><span>embed<span className="accent">-gamesheet.dev</span></span></Link><nav className="header-links" aria-label="Project links"><Link className="docs-link" href="/">Embed builder ↗</Link><a className="docs-link" href="https://github.com/JoshRKulp/gamesheet-embed-builder" target="_blank" rel="noreferrer">GitHub ↗</a></nav></header><section className="landing-hero"><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p>{description}</p><Link className="primary-button landing-cta" href="/">Open the embed builder <span>→</span></Link></section><section className="landing-panel"><div className="landing-feature-grid">{features.map((feature, index) => <div key={feature}><span>{String(index + 1).padStart(2, "0")}</span><p>{feature}</p></div>)}</div><div className="landing-how"><p className="eyebrow">HOW IT WORKS</p><h2>From season ID to<br /><span>ready-to-paste code.</span></h2><div className="landing-steps">{steps.map((step, index) => <div key={step}><b>{index + 1}</b><p>{step}</p></div>)}</div></div></section><section className="landing-bottom"><h2>Make your GameSheet data<br /><span>fit your website.</span></h2><Link className="primary-button" href="/">Build your embed <span>→</span></Link></section><footer><span>EMBED-GAMESHEET.DEV <b>×</b> GAMESHEET</span><span>Open source on <a href="https://github.com/JoshRKulp/gamesheet-embed-builder" target="_blank" rel="noreferrer">GitHub ↗</a></span></footer></main>;
}
